{
  "name": "ts-project",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "start": "npm install && concurrently \"cd backend && npm install && prisma generate --schema src/prisma/schema.prisma && npx prisma migrate dev --schema src/prisma/schema.prisma && npx prisma db seed && npm run dev\" \"cd frontend && npm install && npm run dev\""
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "concurrently": "^8.2.1"
  }
}

{
  "name": "ts-project",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "start": " npm install -g pnpm && pnpm install && concurrently \"cd backend && pnpm install --production && npx prisma db seed && pnpm start\" \"cd frontend && pnpm install --production && npm run dev\""
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "concurrently": "^8.2.1"
  }
}

{
  "name": "ts-project",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "start": "npx pnpm install --omit=dev && concurrently \"cd backend && npx pnpm install --omit=dev && npx prisma migrate deploy --schema src/prisma/schema.prisma && npx prisma db seed && npm run start\" \"cd frontend && npm install && npm run dev\""
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "concurrently": "8.2.2"
  }
}

{
  "name": "ts-project",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "start": "NODE_OPTIONS=--max-old-space-size=4096 npm install && concurrently \"cd backend && NODE_OPTIONS=--max-old-space-size=4096 npm install --production && npx prisma db seed && npm start\" \"cd frontend && NODE_OPTIONS=--max-old-space-size=4096 npm install --production && npm run dev\""
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "concurrently": "^8.2.1"
  }
}
